module.exports = {
    siteUrl: process.env.SITE_URL || 'https://micklemaher.net', // Your website's URL
    generateRobotsTxt: true, // (Optional) Generates a robots.txt file
    sitemapSize: 10, // Number of URLs per sitemap file
};